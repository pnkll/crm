export const getNewestTasks = (state) =>{
    return state.tasks.newest
}

export const getTasks = (state) => {
    return state.tasks
}

export const getSelectedTask = (state) => {
    return state.tasks.selectedTask
}